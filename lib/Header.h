#pragma once

namespace Sorting
{
	class Merge_Sort
	{
	public:
		static void Merge(double arr[], double aux[], int low, int mid, int high);
	public:
		static void mergesort(double arr[], double aux[], int low, int high);
	};
}
